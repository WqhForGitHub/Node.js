# 150. 后端模板工厂

Koa + TypeScript demo，组合多种后端模板，不同类型生成不同文件结构（microservice 多 service 目录，crud-service 按 entities 生成 model/controller）

## 运行

```bash
npm install
npm run dev
```

> 纯 Koa + TypeScript demo。
