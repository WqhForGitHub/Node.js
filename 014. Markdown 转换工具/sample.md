# Markdown 转换工具示例

这是一个 **Markdown 转 HTML** 的示例文件，用于测试各种 Markdown 语法。

## 基础语法

### 文本样式

这是 **粗体文本**，这是 *斜体文本*，这是 ***粗斜体***，这是 ~~删除线~~。

行内代码示例：`const x = 42;`

### 链接与图片

这是一个 [链接示例](https://example.com)

![示例图片](https://via.placeholder.com/150)

## 列表

### 无序列表

- 第一项
- 第二项
- 第三项

### 有序列表

1. 步骤一
2. 步骤二
3. 步骤三

## 引用

> 这是一段引用文本。
> Markdown 是一种轻量级标记语言。

## 代码块

```javascript
function hello(name) {
  console.log(`Hello, ${name}!`);
  return { message: 'done' };
}
```

## 表格

| 功能 | 状态 | 优先级 |
| ---: | :---: | :--- |
| 解析标题 | 已完成 | 高 |
| 解析列表 | 已完成 | 高 |
| 解析表格 | 已完成 | 中 |
| 解析代码块 | 已完成 | 高 |

---

## 混合内容

这是一段包含 **多种** `格式` 的文字，还包含 [链接](https://example.com)。

> 引用中也可以有 **粗体** 和 *斜体*。
